import { Request, Response } from "express";

export default class TicketController {
    public static async getTicketStatus(req: Request, res: Response) {

    }

    public static async getMessageByTicket(req: Request, res: Response) {
   
    }
}